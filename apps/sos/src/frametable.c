/*
 * Frametable implementation
 * Glenn McGuire and Cameron Lonsdale
 */

void frame_alloc(seL4_Word *vaddr) {
	// TODO: the physical memory is reserved via the ut_alloc
	// TODO: the memory is retyped into a frame
	// TODO: and the frame is mapped into the SOS window at a fixed offset of the physical address.
}
